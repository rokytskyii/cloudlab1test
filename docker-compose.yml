version: '3.4'

services:
  smartphonestore.api:
    image: smartphonestore-api
    build:
      context: .
      dockerfile: SmartphoneStore.Api/Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development